{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/srikar/Coding Projects/Scalera\" && git add -A && git diff --cached --quiet || (git commit -m \"Auto-save: $(date '+%Y-%m-%d %H:%M:%S')\" && git push origin srikar) 2>/dev/null || true",
            "statusMessage": "Pushing changes to GitHub..."
          }
        ]
      }
    ]
  }
}
